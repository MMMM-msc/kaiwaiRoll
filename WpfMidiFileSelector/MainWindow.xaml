<Window x:Class="WpfMidiFileSelector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMidiFileSelector"
        mc:Ignorable="d"
        Title="MiDIファイル選択と表示" Height="700" Width="800"> <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="10" VerticalAlignment="Top" Grid.Row="0">
            <Label Content="MIDIファイルパス:" />
            <TextBox Name="filePathTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal">
                <Button Content="ファイルを選択して表示" Click="SelectFileButton_Click" Margin="0,0,10,0"/>
                <Button Name="playButton" Content="再生" Click="PlayButton_Click" Margin="0,0,5,0" IsEnabled="False"/>
                <Button Name="stopButton" Content="停止" Click="StopButton_Click" IsEnabled="False"/>
            </StackPanel>

            <StackPanel Margin="0,10,0,0"> <Label Content="表示色の設定:" FontWeight="Bold"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="背景色:"/>
                    <ComboBox Name="backgroundColorComboBox" Width="150" SelectionChanged="ColorComboBox_SelectionChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="通常ノート色:"/>
                     <ComboBox Name="normalNoteColorComboBox" Width="150" SelectionChanged="ColorComboBox_SelectionChanged"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Label Content="再生ノート色:"/>
                     <ComboBox Name="playingNoteColorComboBox" Width="150" SelectionChanged="ColorComboBox_SelectionChanged"/>
                </StackPanel>

            </StackPanel>
            </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="10"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Canvas Name="pianoRollCanvas"/> </ScrollViewer>
    </Grid>
</Window>